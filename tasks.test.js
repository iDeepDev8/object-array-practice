const displayListeners = require('./tasks')

test('displayListeners display listeners from listeners.js file', () => {
    // Arrange
    const arrayListener = []
    const listen = arrayListener.push(listeners)
    // Act
    const actual = 
    // Assert
})