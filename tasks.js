// Display the names of all of the listeners

const listeners = require('./data/listeners')

function displayListeners() {
    const arrayListener = []
    const listen = arrayListener.push(listeners)
    return listen
}

module.exports = displayListeners